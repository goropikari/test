services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    tty: true

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sample
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
